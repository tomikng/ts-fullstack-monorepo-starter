{
  "name": "fullstack-monorepo-starter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:web": "pnpm --filter web dev",
    "dev:backend": "pnpm --filter backend start:dev",
    "dev": "pnpm --filter web dev & pnpm --filter backend start:dev",
    "build:web": "pnpm --filter web build",
    "build:backend": "pnpm --filter backend build",
    "build": "pnpm --filter web build & pnpm --filter backend build",
    "migrate:dev": "pnpm --filter database prisma migrate dev",
    "generate:prisma": "pnpm --filter database prisma generate",
    "studio": "pnpm --filter database prisma studio"
  },
  "devDependencies": {
    "@workspace/eslint-config": "workspace:*",
    "@workspace/typescript-config": "workspace:*",
    "prettier": "^3.2.5",
    "turbo": "^2.3.0",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.1.3",
  "engines": {
    "node": ">=20"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
